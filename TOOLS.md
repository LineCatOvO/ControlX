# 工具文档

## 可用工具列表

### 核心开发工具

1. **Read** - 读取文件内容
   - 功能：读取本地文件系统中的文件
   - 范围：可访问项目内所有文件
   - 限制：只能读取文件，不能读取目录

2. **Write** - 写入文件内容
   - 功能：向本地文件系统写入文件
   - 范围：可创建或覆盖项目内文件
   - 限制：必须先读取文件才能编辑，倾向于编辑而非新建

3. **Edit** - 编辑文件内容
   - 功能：对现有文件进行字符串替换
   - 范围：修改项目内现有文件
   - 限制：需要精确匹配原字符串，支持批量替换

4. **Glob** - 文件模式匹配搜索
   - 功能：按模式搜索匹配的文件
   - 范围：整个项目目录结构
   - 限制：使用标准 glob 模式语法

5. **Grep** - 内容搜索工具
   - 功能：在文件中搜索指定内容
   - 范围：支持正则表达式搜索
   - 限制：基于 ripgrep 引擎

### 系统操作工具

6. **Bash** - 执行终端命令
   - 功能：执行系统命令和脚本
   - 范围：完整的 shell 环境访问
   - 限制：避免用于文件操作，主要用于系统命令

2. **CheckRuntime** - 检查运行环境
   - 功能：检测各种运行时环境安装状态
   - 范围：Node.js, Python, Ruby, Java, Go, Rust 等
   - 限制：仅检测不安装

### 项目管理工具

8. **TodoWrite** - 任务列表管理
   - 功能：创建和管理结构化任务列表
   - 范围：当前会话的任务跟踪
   - 限制：适用于复杂多步骤任务

2. **AskUserQuestion** - 用户交互工具
   - 功能：向用户提问获取信息
   - 范围：澄清需求、确认选择、获取偏好
   - 限制：最多一次提4个问题

### 网络工具

10. **WebFetch** - 网页内容获取
    - 功能：获取并处理网页内容
    - 范围：公开可访问的网页URL
    - 限制：内容会被AI模型处理总结

2. **WebSearch** - 网络搜索
    - 功能：搜索网络获取最新信息
    - 范围：搜索引擎结果
    - 限制：受搜索服务限制

### 特殊工具

12. **Task** - 启动专用代理
    - 功能：启动专门处理复杂任务的子代理
    - 范围：代码审查、通用研究等专业任务
    - 限制：需要明确指定代理类型

2. **EnterSpecMode/ExitSpecMode** - 规划模式控制
    - 功能：进入/退出实现规划模式
    - 范围：复杂任务的前期规划阶段
    - 限制：需要用户同意进入规划模式

## 工具使用原则

- 优先使用专用工具而非通用命令
- 文件操作优先使用 Read/Edit/Write 而非 Bash
- 复杂搜索使用 Glob/Grep 而非手动遍历
- 重要决策前使用 AskUserQuestion 确认
