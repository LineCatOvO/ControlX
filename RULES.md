# 项目规则文档

## 文档管理规则
- 文档是唯一可信状态来源，任何未写入文档的内容视为不存在
- 所有规则性内容必须写入此文件
- 所有"必须 / 禁止 / 应当 / 不可"等约束性内容必须在此记录

## 开发流程规则
1. RULES.md 优先级最高，规则优先于任务
2. 仅执行 TASKS.md 中存在或已补充的任务
3. 每轮只推进最小且可验证的一步
4. 不得假设完成，必须基于实际结果
5. 执行过程中必须即时写入文档，不得仅在结束时总结
6. 禁止在上下文中保留任何未写入文档的信息

## 信息分类规则
必须从用户提示词与执行过程中自动识别并分类信息：
- 规则性内容 → RULES.md
- 任务性内容 → TASKS.md  
- 可复用知识 → KNOWLEDGE.md
- 临时/未整理内容 → DRAFT.md
- 工具相关信息 → TOOLS.md

## 执行前查询规则
在执行任何操作前，必须：
- 从当前任务中提取关键词
- 在所有项目文档中查询相关内容
- 若已有相关规则、任务、知识或工具说明，必须遵循并引用
- 若不存在，执行后必须补充写入对应文档

## 终止条件
仅在明确满足项目目标或收到停止指令时终止，否则持续循环