Feature: 操作布局管理
  As a 赛车游戏玩家
  I want to 创建、编辑和管理操作布局
  So that 我可以自定义控制方式以适应不同游戏

  Background: 应用已启动
    Given 应用已启动
    And 至少存在一个布局配置

  Scenario: 查看布局列表
    Given 应用已启动
    When 用户进入布局管理界面
    Then 系统应显示所有已保存的布局列表
    And 每个布局应显示名称和简要描述
    And 当前激活的布局应有明确标识

  Scenario: 创建新布局
    Given 用户在布局管理界面
    When 用户点击"创建新布局"按钮
    And 用户输入布局名称"我的自定义布局"
    And 用户确认创建
    Then 系统应创建名为"我的自定义布局"的新布局
    And 新布局应出现在布局列表中
    And 新布局应包含默认的操作元素

  Scenario: 删除布局
    Given 布局列表中存在多个布局
    And 用户选中布局"测试布局"
    When 用户点击"删除"按钮
    And 用户确认删除操作
    Then 系统应删除"测试布局"
    And 布局列表中不再显示该布局
    And 其他布局应保持不变

  Scenario: 删除当前激活的布局
    Given 布局"默认布局"是当前激活的布局
    And 布局列表中存在其他布局
    When 用户删除"默认布局"
    Then 系统应自动切换到另一个布局
    And 不应出现无布局激活的状态

  Scenario: 删除最后一个布局
    Given 布局列表中只有一个布局
    When 用户尝试删除该布局
    Then 系统应提示无法删除最后一个布局
    And 布局应保持不变

  Scenario: 切换布局
    Given 布局"布局A"是当前激活的布局
    And 布局"布局B"存在于布局列表中
    When 用户选择切换到"布局B"
    Then 系统应立即切换到"布局B"
    And 控制行为应立即按新布局生效
    And 不应产生残留控制状态
    And 连接状态应保持不变

  Scenario: 布局切换时控制状态
    Given 服务状态为"已启动"
    And 当前布局有按键处于按下状态
    When 用户切换到新布局
    Then 所有按键状态应被清零
    And 新布局的控制映射应立即生效
    And 后端应收到状态清零指令

  Scenario: 进入布局编辑模式
    Given 用户在布局管理界面
    And 用户选中布局"我的布局"
    When 用户点击"编辑"按钮
    Then 系统应进入布局编辑模式
    And 编辑界面应显示当前布局的所有操作元素
    And 编辑界面应提供编辑工具栏

  Scenario: 添加操作元素
    Given 用户在布局编辑模式
    When 用户点击"添加元素"按钮
    And 选择元素类型"按键"
    And 设置元素属性（位置、大小、映射键位）
    And 用户确认添加
    Then 新的操作元素应出现在布局中
    And 元素属性应按设置保存

  Scenario: 删除操作元素
    Given 用户在布局编辑模式
    And 布局中存在多个操作元素
    When 用户选中一个元素
    And 用户点击"删除"按钮
    Then 该元素应从布局中移除
    And 其他元素应保持不变

  Scenario: 调整元素位置
    Given 用户在布局编辑模式
    And 布局中存在一个按键元素
    When 用户拖动该元素到新位置
    Then 元素应移动到新位置
    And 元素的新位置应被保存

  Scenario: 调整元素大小
    Given 用户在布局编辑模式
    And 布局中存在一个摇杆元素
    When 用户调整元素的大小
    Then 元素应按新大小显示
    And 元素的新大小应被保存

  Scenario: 编辑元素控制映射
    Given 用户在布局编辑模式
    And 用户选中一个按键元素
    When 用户修改该按键映射到"空格键"
    Then 按键映射应更新为"空格键"
    And 保存后触控该区域应产生空格键输出

  Scenario: 编辑元素参数
    Given 用户在布局编辑模式
    And 用户选中一个摇杆元素
    When 用户修改灵敏度参数为0.8
    And 用户修改死区参数为0.15
    Then 摇杆参数应更新
    And 保存后摇杆应按新参数工作

  Scenario: 实时预览编辑效果
    Given 用户在布局编辑模式
    And 服务状态为"已启动"
    When 用户修改元素属性
    Then 用户应能实时看到控制输出的变化
    And 预览不应影响实际控制输出（除非确认应用）

  Scenario: 放弃编辑修改
    Given 用户在布局编辑模式
    And 用户已进行多处修改
    When 用户点击"取消"按钮
    Then 系统应放弃所有未保存的修改
    And 布局应恢复到编辑前的状态

  Scenario: 保存编辑修改
    Given 用户在布局编辑模式
    And 用户已进行多处修改
    When 用户点击"保存"按钮
    Then 系统应保存所有修改
    And 布局应更新为新版本
    And 用户应返回布局管理界面

  Scenario: 导出布局
    Given 用户在布局管理界面
    And 用户选中布局"我的布局"
    When 用户点击"导出"按钮
    Then 系统应生成布局导出文件
    And 用户应能选择保存位置
    And 导出文件应包含完整的布局配置

  Scenario: 导入布局
    Given 用户在布局管理界面
    And 存在有效的布局导出文件
    When 用户点击"导入"按钮
    And 选择布局文件
    Then 系统应解析布局文件
    And 新布局应出现在布局列表中
    And 导入的布局应可正常使用

  Scenario: 导入无效布局文件
    Given 用户在布局管理界面
    And 存在无效的布局文件
    When 用户尝试导入该文件
    Then 系统应检测到文件无效
    And 系统应显示错误提示
    And 现有布局应不受影响

  Scenario: 布局草稿保存
    Given 用户在布局编辑模式
    And 用户已进行修改但未完成
    When 用户选择"保存为草稿"
    Then 系统应以"草稿-"前缀保存布局
    And 草稿布局应与正式布局区分显示
    And 用户可以稍后继续编辑

  Scenario: 布局备份
    Given 用户在布局编辑模式
    And 用户准备进行重大修改
    When 用户选择"创建备份"
    Then 系统应以"备份-"前缀保存当前布局副本
    And 用户可以随时恢复到备份版本

  Scenario: 恢复布局备份
    Given 存在布局备份"备份-我的布局"
    When 用户选择恢复该备份
    Then 当前布局应被备份内容覆盖
    And 布局应恢复到备份时的状态

  Scenario: 编辑不中断控制运行
    Given 服务状态为"已启动"
    And 用户进入布局编辑模式
    When 用户进行编辑操作
    Then 控制服务应继续运行
    And 当前激活的布局应继续生效
    And 编辑中的修改不应立即影响控制输出